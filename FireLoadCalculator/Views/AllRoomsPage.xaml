<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:strings="clr-namespace:FireLoadCalculator.Resources.Strings"
             
             x:Class="FireLoadCalculator.Views.AllRoomsPage"
             xmlns:model="clr-namespace:FireLoadCalculator.Models"
             xmlns:viewmodel="clr-namespace:FireLoadCalculator.ViewModels"
             x:DataType="viewmodel:AllRoomsViewModel"
             >

    <Grid ColumnDefinitions="33*, 33*, 33*" RowDefinitions="64, *, 64" RowSpacing="4">
        <Label Text="{x:Static strings:AppResources.AllRoomsName}" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" FontSize="24" Grid.Column="0" Margin="4"/>
        <Label Text="{x:Static strings:AppResources.AllRoomsArea}" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" FontSize="24" Grid.Column="1" Margin="4"/>
        <Label Text="{x:Static strings:AppResources.AllRoomsTotalFireLoad}" HorizontalTextAlignment="Center" VerticalTextAlignment="Center" FontSize="24" Grid.Column="2" Margin="4"/>
        
        <BoxView Grid.Column="0" Grid.Row="1" Color="#040404" Margin="4" Grid.ColumnSpan="3" />
        <CollectionView Grid.Column="0" Grid.Row="1" ItemsSource="{Binding Rooms}" Grid.ColumnSpan="3" Margin="4">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="model:Room">
                    <Grid ColumnDefinitions="33*, 33*, 33*" RowDefinitions="48" >
                        <BoxView Grid.Column="0" Color="#080808" Margin="2"/>
                        <Label Text="{Binding Name}" FontSize="24" Grid.Column="0" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" Margin="4"/>
                        <BoxView Grid.Column="1" Color="#0F0F0F" Margin="2"/>
                        <Label Text="{Binding Area}" FontSize="24" Grid.Column="1" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" Margin="4"/>
                        <BoxView Grid.Column="2" Color="#080808" Margin="2" />
                        <Label Text="TODO" FontSize="24" Grid.Column="2" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" Margin="4"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            <CollectionView.EmptyView>
                <ContentView>
                    <Label Text="EMPTY" FontSize="24" Grid.Column="0" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" Margin="4"/>
                </ContentView>
            </CollectionView.EmptyView>
        </CollectionView>

        <Button Text="{x:Static strings:AppResources.AllRoomsControlsAdd}" Command="{Binding DisplayPopupCommand}" Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="3" Margin="4"/>
    </Grid>
</ContentPage>
